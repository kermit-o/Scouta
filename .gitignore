.env
*.jar
*.tsbuildinfo
session_debug_dump.txt
main
": post upvote*
test_qwen_client.py
blog-ai/blog-saas/main
node_modules/

# Local SQLite dev DB — never commit (contains random local state, churns on every run)
dev.db
*.db
__pycache__/
.pytest_cache/

# Editor / tool snapshot backups — never commit (we use git for history)
*.bak
*.bak_*
*.backup
*.backup_*

# Local secret backups — never commit
.env.bak
.env.backup
.env.local
.env.*.local
